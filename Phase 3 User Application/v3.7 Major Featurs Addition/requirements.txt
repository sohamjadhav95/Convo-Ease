# ConvoEase v3.6 - Python Dependencies
# Install with: pip install -r requirements.txt

# Web framework
flask>=3.0.0
flask-cors>=4.0.0

# Data handling
pandas>=2.0.0

# AI / API client
openai>=1.30.0
langdetect>=1.0.9
deep-translator>=1.11.4

# Audio transcription helpers
SpeechRecognition>=3.10.0
pydub>=0.25.1

# Environment variables
python-dotenv>=1.0.0

# Testing
pytest>=8.0.0
pytest-asyncio>=0.23.0
playwright>=1.50.0
locust>=2.31.0

# Optional local model runtime for hybrid mode
# Install these only if you want to run local Text / Image / Audio models.
# torch
# transformers>=4.45.0
# pillow>=10.0.0
# soundfile>=0.12.1
# numpy>=1.26.0

# Notes
# - pydub requires ffmpeg to be installed and available on PATH.
# - Standard library modules such as uuid, os, sys, base64, io, datetime,
#   and logging do not need separate installation.
